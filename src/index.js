import menuToggle from "./menu"
import initiateControls from "./calendar"



menuToggle()
initiateControls()






/*const Project = (name) => {
    return {name};
}

const Task = (name, date, priority) => {
    return {name, date, priority};
}

const task1 = Task("hi", "2", "low")
const task2 = Task("TEST", "5", "high")

let myProjects = [];
let myTasks = [];

myTasks = [task1, task2]
myProjects[Project] = myTasks

console.log(myProjects)*/